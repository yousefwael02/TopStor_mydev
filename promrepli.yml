# Prometheus configuration file

global:
  scrape_interval: 1m
  evaluation_interval: 1m

scrape_configs:
  - job_name: 'federated'
    scrape_interval: 1m
    static_configs:
      - targets: ['LEADERIP:9090'] 
